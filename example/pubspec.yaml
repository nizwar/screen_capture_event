name: screen_capture_event_example
description: Demonstrates how to use the screen_capture_event plugin.
publish_to: none

environment:
  sdk: ">=2.17.0-0 <3.0.0"
dependencies:
  flutter:
    sdk: flutter

  screen_capture_event:
    path: ../
  cupertino_icons: ^1.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1
flutter:
  uses-material-design: true 